import { ActivesidebarDirective } from './activesidebar.directive';

describe('ActivesidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivesidebarDirective();
    expect(directive).toBeTruthy();
  });
});
